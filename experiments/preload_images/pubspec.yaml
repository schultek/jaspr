name: preload_images_experiment
version: 0.1.0
publish_to: none

resolution: workspace
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.0.0
  jaspr: ^0.13.0
  jaspr_router: ^0.4.2

dev_dependencies:
  build_runner: ^2.4.0
  build_web_compilers: ^4.0.0
  lints: ^2.1.0
