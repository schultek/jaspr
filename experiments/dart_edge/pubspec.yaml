name: dart_edge
description: A purely server-side rendered app without client hydration.
version: 0.0.1

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  edge: ^0.0.1
  jaspr: ^0.2.0
  vercel_edge: ^0.0.1

dev_dependencies:
  build_runner: ^2.2.0
  build_web_compilers: ^3.2.1
  jaspr_builder: ^0.2.0
  lints: ^2.0.1
  webdev: 2.7.7
