name: jaspr_edge
description: A jaspr app running on the edge.
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  edge: ^0.0.6
  jaspr: ^0.5.0
  vercel_edge: ^0.0.4

dev_dependencies:
  build_runner: ^2.4.0
  build_web_compilers: ^4.0.0
  jaspr_builder: ^0.5.0
  lints: ^2.1.0
