import '../../../jaspr.dart';

class PlatformHead extends StatelessComponent {
  const PlatformHead({super.key});

  @override
  Iterable<Component> build(BuildContext context) sync* {
    yield div([]);
  }
}
