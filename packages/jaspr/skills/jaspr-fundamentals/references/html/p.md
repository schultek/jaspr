# p

Signature of the p component:

```dart
const p(List<Component> children, {
  String? id,
  String? classes,
  Styles? styles,
  Map<String, String>? attributes,
  Map<String, EventCallback>? events,
  Key? key,
})
```

Example usage:

```dart
p(classes: '...', [
  // ...
])
```