# strong

Signature of the strong component:

```dart
const strong(List<Component> children, {
  String? id,
  String? classes,
  Styles? styles,
  Map<String, String>? attributes,
  Map<String, EventCallback>? events,
  Key? key,
})
```

Example usage:

```dart
strong(classes: '...', [
  // ...
])
```