# dd

Signature of the dd component:

```dart
const dd(List<Component> children, {
  String? id,
  String? classes,
  Styles? styles,
  Map<String, String>? attributes,
  Map<String, EventCallback>? events,
  Key? key,
})
```

Example usage:

```dart
dd(classes: '...', [
  // ...
])
```