# aside

Signature of the aside component:

```dart
const aside(List<Component> children, {
  String? id,
  String? classes,
  Styles? styles,
  Map<String, String>? attributes,
  Map<String, EventCallback>? events,
  Key? key,
})
```

Example usage:

```dart
aside(classes: '...', [
  // ...
])
```