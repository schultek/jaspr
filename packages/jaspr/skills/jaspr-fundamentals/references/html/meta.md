# meta

Signature of the meta component:

```dart
const meta({
  String? name,
  String? content,
  String? charset,
  String? httpEquiv,
  String? id,
  String? classes,
  Styles? styles,
  Map<String, String>? attributes,
  Map<String, EventCallback>? events,
  Key? key,
})
```

Example usage:

```dart
meta(name: '...')
```