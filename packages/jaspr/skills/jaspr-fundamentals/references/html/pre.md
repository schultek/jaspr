# pre

Signature of the pre component:

```dart
const pre(List<Component> children, {
  String? id,
  String? classes,
  Styles? styles,
  Map<String, String>? attributes,
  Map<String, EventCallback>? events,
  Key? key,
})
```

Example usage:

```dart
pre(classes: '...', [
  // ...
])
```