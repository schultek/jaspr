# thead

Signature of the thead component:

```dart
const thead(List<Component> children, {
  String? id,
  String? classes,
  Styles? styles,
  Map<String, String>? attributes,
  Map<String, EventCallback>? events,
  Key? key,
})
```

Example usage:

```dart
thead(classes: '...', [
  // ...
])
```