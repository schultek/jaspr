# h4

Signature of the h4 component:

```dart
const h4(List<Component> children, {
  String? id,
  String? classes,
  Styles? styles,
  Map<String, String>? attributes,
  Map<String, EventCallback>? events,
  Key? key,
})
```

Example usage:

```dart
h4(classes: '...', [
  // ...
])
```