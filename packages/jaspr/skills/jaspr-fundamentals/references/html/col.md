# col

Signature of the col component:

```dart
const col({
  int? span,
  String? id,
  String? classes,
  Styles? styles,
  Map<String, String>? attributes,
  Map<String, EventCallback>? events,
  Key? key,
})
```

Example usage:

```dart
col(span: 1)
```