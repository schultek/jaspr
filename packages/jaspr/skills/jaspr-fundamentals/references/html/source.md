# source

Signature of the source component:

```dart
const source({
  String? src,
  String? type,
  String? id,
  String? classes,
  Styles? styles,
  Map<String, String>? attributes,
  Map<String, EventCallback>? events,
  Key? key,
})
```

Example usage:

```dart
source(src: '...')
```