# summary

Signature of the summary component:

```dart
const summary(List<Component> children, {
  String? id,
  String? classes,
  Styles? styles,
  Map<String, String>? attributes,
  Map<String, EventCallback>? events,
  Key? key,
})
```

Example usage:

```dart
summary(classes: '...', [
  // ...
])
```