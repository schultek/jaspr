# i

Signature of the i component:

```dart
const i(List<Component> children, {
  String? id,
  String? classes,
  Styles? styles,
  Map<String, String>? attributes,
  Map<String, EventCallback>? events,
  Key? key,
})
```

Example usage:

```dart
i(classes: '...', [
  // ...
])
```