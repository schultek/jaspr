# tfoot

Signature of the tfoot component:

```dart
const tfoot(List<Component> children, {
  String? id,
  String? classes,
  Styles? styles,
  Map<String, String>? attributes,
  Map<String, EventCallback>? events,
  Key? key,
})
```

Example usage:

```dart
tfoot(classes: '...', [
  // ...
])
```