# b

Signature of the b component:

```dart
const b(List<Component> children, {
  String? id,
  String? classes,
  Styles? styles,
  Map<String, String>? attributes,
  Map<String, EventCallback>? events,
  Key? key,
})
```

Example usage:

```dart
b(classes: '...', [
  // ...
])
```