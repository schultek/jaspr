# tbody

Signature of the tbody component:

```dart
const tbody(List<Component> children, {
  String? id,
  String? classes,
  Styles? styles,
  Map<String, String>? attributes,
  Map<String, EventCallback>? events,
  Key? key,
})
```

Example usage:

```dart
tbody(classes: '...', [
  // ...
])
```