# h5

Signature of the h5 component:

```dart
const h5(List<Component> children, {
  String? id,
  String? classes,
  Styles? styles,
  Map<String, String>? attributes,
  Map<String, EventCallback>? events,
  Key? key,
})
```

Example usage:

```dart
h5(classes: '...', [
  // ...
])
```