# section

Signature of the section component:

```dart
const section(List<Component> children, {
  String? id,
  String? classes,
  Styles? styles,
  Map<String, String>? attributes,
  Map<String, EventCallback>? events,
  Key? key,
})
```

Example usage:

```dart
section(classes: '...', [
  // ...
])
```