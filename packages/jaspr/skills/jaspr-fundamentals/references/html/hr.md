# hr

Signature of the hr component:

```dart
const hr({
  String? id,
  String? classes,
  Styles? styles,
  Map<String, String>? attributes,
  Map<String, EventCallback>? events,
  Key? key,
})
```

Example usage:

```dart
hr(classes: '...')
```