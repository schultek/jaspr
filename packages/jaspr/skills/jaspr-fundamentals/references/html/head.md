# head

Signature of the head component:

```dart
const head(List<Component> children, {
  String? id,
  String? classes,
  Styles? styles,
  Map<String, String>? attributes,
  Map<String, EventCallback>? events,
  Key? key,
})
```

Example usage:

```dart
head(classes: '...', [
  // ...
])
```