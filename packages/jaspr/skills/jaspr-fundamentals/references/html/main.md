# main_

Signature of the main_ component:

```dart
const main_(List<Component> children, {
  String? id,
  String? classes,
  Styles? styles,
  Map<String, String>? attributes,
  Map<String, EventCallback>? events,
  Key? key,
})
```

Example usage:

```dart
main_(classes: '...', [
  // ...
])
```