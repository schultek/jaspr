# dt

Signature of the dt component:

```dart
const dt(List<Component> children, {
  String? id,
  String? classes,
  Styles? styles,
  Map<String, String>? attributes,
  Map<String, EventCallback>? events,
  Key? key,
})
```

Example usage:

```dart
dt(classes: '...', [
  // ...
])
```