# caption

Signature of the caption component:

```dart
const caption(List<Component> children, {
  String? id,
  String? classes,
  Styles? styles,
  Map<String, String>? attributes,
  Map<String, EventCallback>? events,
  Key? key,
})
```

Example usage:

```dart
caption(classes: '...', [
  // ...
])
```