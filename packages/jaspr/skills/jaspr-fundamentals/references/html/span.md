# span

Signature of the span component:

```dart
const span(List<Component> children, {
  String? id,
  String? classes,
  Styles? styles,
  Map<String, String>? attributes,
  Map<String, EventCallback>? events,
  Key? key,
})
```

Example usage:

```dart
span(classes: '...', [
  // ...
])
```