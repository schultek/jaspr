# legend

Signature of the legend component:

```dart
const legend(List<Component> children, {
  String? id,
  String? classes,
  Styles? styles,
  Map<String, String>? attributes,
  Map<String, EventCallback>? events,
  Key? key,
})
```

Example usage:

```dart
legend(classes: '...', [
  // ...
])
```