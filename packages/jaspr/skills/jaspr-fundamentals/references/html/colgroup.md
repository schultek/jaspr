# colgroup

Signature of the colgroup component:

```dart
const colgroup(List<Component> children, {
  int? span,
  String? id,
  String? classes,
  Styles? styles,
  Map<String, String>? attributes,
  Map<String, EventCallback>? events,
  Key? key,
})
```

Example usage:

```dart
colgroup(span: 1, [
  // ...
])
```