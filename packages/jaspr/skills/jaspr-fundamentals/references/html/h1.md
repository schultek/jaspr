# h1

Signature of the h1 component:

```dart
const h1(List<Component> children, {
  String? id,
  String? classes,
  Styles? styles,
  Map<String, String>? attributes,
  Map<String, EventCallback>? events,
  Key? key,
})
```

Example usage:

```dart
h1(classes: '...', [
  // ...
])
```