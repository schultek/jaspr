name: jaspr_devtools
description: Standalone DevTools app for Jaspr — inspect component tree, state, and metadata.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.8.0 <4.0.0'

resolution: workspace

dependencies:
  jaspr: any
  universal_web: any

jaspr:
  mode: client
