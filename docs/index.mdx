import React from 'react'; import { Card, CardContent } from "@/components/ui/card"; import { Button } from "@/components/ui/button"; import { ShoppingCart, Smartphone, Search } from 'lucide-react';

export default function PhoneStoreApp() { return ( <div className="min-h-screen bg-black text-white"> {/* Header */} <header className="flex items-center justify-between p-4 border-b border-gray-800"> <h1 className="text-2xl font-bold text-white">📱 متجر الهواتف | Phone Store</h1> <div className="flex items-center gap-3"> <Search className="w-6 h-6" /> <ShoppingCart className="w-6 h-6" /> </div> </header>

{/* Hero Section */}
  <section className="p-6 text-center">
    <h2 className="text-3xl font-semibold mb-2">أفضل الهواتف بأفضل الأسعار</h2>
    <p className="text-gray-400 mb-4">هواتف جديدة ومستعملة من جميع الماركات العالمية</p>
    <Button className="bg-white text-black font-bold px-6 py-2 rounded-full hover:bg-gray-300 transition">تسوق الآن</Button>
  </section>

  {/* Products Grid */}
  <main className="grid grid
